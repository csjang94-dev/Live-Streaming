# 🎥 라이브 스트리밍 웹 플레이어 사용 가이드

## 📥 파일 다운로드

`live-stream-player.html` 파일을 다운로드하세요!

## 🚀 사용 방법

### 1️⃣ HLS URL 확인

터미널에서 실행:

```bash
cd complete-terraform/environments/dev
terraform output hls_s3_url
```

**출력 예시:**
```
https://live-streaming-dev-hls-140023399909.s3.ap-northeast-2.amazonaws.com/live/index.m3u8
```

이 URL을 복사하세요! 📋

### 2️⃣ MediaLive 채널 시작

```bash
# Lambda로 시작
terraform output start_channel_command | bash

# 30초 대기
sleep 30
```

### 3️⃣ Larix로 스트리밍 시작

1. **Larix Broadcaster** 앱 열기
2. **RTMP URL** 입력:
   ```bash
   terraform output medialive_rtmp_destinations
   ```
3. **Stream Key:** `stream-key-1`
4. **🔴 버튼** 눌러 스트리밍 시작

### 4️⃣ 웹 플레이어 열기

1. `live-stream-player.html` 파일을 **더블클릭**
2. 브라우저에서 열림
3. **HLS 스트림 URL** 입력창에 1️⃣의 URL 붙여넣기
4. **▶️ 스트림 시작** 버튼 클릭
5. 재생 시작! 🎉

## ✨ 주요 기능

### 📺 자동 재생
- URL 입력하고 버튼만 누르면 자동 재생
- 마지막 URL 자동 저장 (다음에 열 때 자동 입력)

### 🔄 자동 복구
- 네트워크 끊김 → 자동 재연결
- 미디어 오류 → 자동 복구

### 📱 반응형 디자인
- PC, 태블릿, 모바일 모두 지원
- 세로/가로 모드 자동 적응

### 🎨 실시간 상태 표시
- 🔴 오프라인
- 🟡 연결 중
- 🟢 LIVE

### 🖥️ 브라우저 호환성
- ✅ Chrome
- ✅ Firefox
- ✅ Edge
- ✅ Safari (iOS 포함)

## 🎯 테스트 체크리스트

- [ ] 1. Terraform 배포 완료
- [ ] 2. HLS URL 확인
- [ ] 3. MediaLive 채널 시작
- [ ] 4. Larix 스트리밍 시작
- [ ] 5. 웹 플레이어 열기
- [ ] 6. URL 입력 및 재생
- [ ] 7. 영상/소리 확인

## 🔧 트러블슈팅

### 문제 1: "스트림 오프라인" 표시

**원인:**
- MediaLive 채널이 중지됨
- Larix 스트리밍 안 함
- URL 오류

**해결:**
```bash
# 채널 상태 확인
terraform output check_status_command | bash

# 출력: "RUNNING"이어야 함
# "IDLE"이면 채널 시작 필요
```

### 문제 2: 재생 안 됨

**체크리스트:**
1. MediaLive 채널: RUNNING ✅
2. Larix 스트리밍: ON ✅
3. 30초 이상 대기 (세그먼트 생성) ✅
4. URL 정확성 ✅

**S3에 파일 확인:**
```bash
BUCKET=$(terraform output -raw hls_bucket_name)
aws s3 ls s3://$BUCKET/live/

# 출력: index.m3u8, *.ts 파일들이 보여야 함
```

### 문제 3: 로딩만 계속됨

**원인:** 아직 세그먼트 생성 안 됨

**해결:** 30~60초 더 대기 후 새로고침

### 문제 4: CORS 오류

**증상:** 브라우저 콘솔에 CORS 에러

**해결:** 이미 terraform에 CORS 설정 포함됨
```bash
# 확인
aws s3api get-bucket-cors --bucket $(terraform output -raw hls_bucket_name)
```

## 🌐 GitHub Pages로 호스팅 (선택사항)

웹에서 접근 가능하게 만들기:

### 1. GitHub 저장소 생성
```bash
git init
git add live-stream-player.html
git commit -m "Add live stream player"
git remote add origin https://github.com/yourusername/live-stream.git
git push -u origin main
```

### 2. GitHub Pages 활성화
1. GitHub 저장소 → Settings
2. Pages 메뉴
3. Source: `main` branch
4. Save

### 3. 접근
```
https://yourusername.github.io/live-stream/live-stream-player.html
```

이제 어디서든 링크로 공유 가능! 🎉

## 💡 커스터마이징

### 색상 변경

HTML 파일 열고 CSS 수정:

```css
/* 그라데이션 색상 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 원하는 색으로 변경 */
background: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 100%);
```

### 로고 추가

```html
<div class="header">
  <img src="your-logo.png" alt="Logo" style="height: 50px;">
  <h1>🎥 Live Streaming</h1>
  ...
</div>
```

### 기본 URL 설정

```javascript
// 페이지 로드 시 저장된 URL 불러오기
window.addEventListener('load', () => {
  const savedUrl = localStorage.getItem('streamUrl');
  if (savedUrl) {
    urlInput.value = savedUrl;
  } else {
    // 기본 URL 설정
    urlInput.value = 'YOUR_DEFAULT_URL_HERE';
  }
});
```

## 📸 스크린샷

플레이어 화면:
```
┌────────────────────────────────────┐
│   🎥 Live Streaming                │
│   🟢 LIVE                          │
├────────────────────────────────────┤
│                                    │
│        [비디오 화면]                │
│                                    │
├────────────────────────────────────┤
│ 🔗 HLS 스트림 URL                  │
│ [https://bucket.s3...m3u8]        │
│                                    │
│      [▶️ 스트림 시작]               │
│                                    │
│ 📝 사용 방법: ...                  │
└────────────────────────────────────┘
```

## 🎉 완성!

이제:
- ✅ 웹 브라우저에서 재생
- ✅ 모바일에서 재생
- ✅ 링크 공유 가능
- ✅ 포트폴리오에 추가

**다음 단계:**
1. 친구에게 공유해보기
2. 포트폴리오에 "라이브 스트리밍 웹앱" 추가
3. GitHub Pages로 배포
4. 이력서/포트폴리오에 링크 추가

축하합니다! 🎊
